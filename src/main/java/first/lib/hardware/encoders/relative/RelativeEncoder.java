package first.lib.hardware.encoders.relative;

import first.lib.hardware.encoders.Encoder;

public abstract class RelativeEncoder extends Encoder {
    
    protected RelativeEncoder(first.lib.hardware.encoders.EncoderConfig cfg) {
        super(cfg);
    }

}